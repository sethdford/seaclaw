{
  "name": "seaclaw",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "cd ui && npm run check && cd ../website && npm run check && cd ../design-tokens && npm run check"
  }
}
