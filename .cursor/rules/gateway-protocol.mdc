---
description: Gateway wire protocol contract for UI-to-C communication
globs:
  - "ui/src/demo-gateway.ts"
  - "src/gateway/**"
  - "ui/src/views/**"
  - "ui/src/controllers/**"
---

- Read `docs/standards/engineering/gateway-api.md` for the gateway API standard.

# Gateway Wire Protocol

The UI communicates with the C gateway over WebSocket using a JSON-RPC style protocol.

## Envelope Format

Request: `{ "method": "noun.verb", "id": "uuid", ...params }`
Response: `{ "id": "uuid", ...result }` or `{ "id": "uuid", "error": "message" }`

## Method Naming

All methods follow `noun.verb` convention (e.g., `chat.send`, `sessions.list`).

## Method Catalog (76 methods)

| Namespace    | Methods |
|-------------|---------|
| `chat`      | `send`, `history`, `abort` |
| `sessions`  | `list`, `delete`, `patch` |
| `channels`  | `status` |
| `tools`     | `catalog` |
| `models`    | `list`, `decisions` |
| `config`    | `get`, `set`, `schema`, `apply` |
| `skills`    | `list`, `search`, `install`, `uninstall`, `enable`, `disable`, `update` (ifdef HU_HAS_SKILLS) |
| `nodes`     | `list`, `action` |
| `agents`    | `list` |
| `cron`      | `list`, `add`, `remove`, `update`, `run`, `runs` (ifdef HU_HAS_CRON) |
| `usage`     | `summary` |
| `activity`  | `recent` |
| `persona`   | `set` |
| `update`    | `check`, `run` (ifdef HU_HAS_UPDATE) |
| `auth`      | `token`, `oauth.start`, `oauth.callback`, `oauth.refresh` |
| `exec`      | `approval.resolve` |
| `push`      | `register`, `unregister` (ifdef HU_HAS_PUSH) |
| `memory`    | `status`, `list`, `recall`, `store`, `forget`, `ingest`, `consolidate`, `graph` |
| `hula`      | `traces.list`, `traces.get`, `traces.delete`, `traces.analytics` |
| `metrics`   | `snapshot` |
| `canvas`    | `list`, `get`, `edit`, `undo`, `redo` |
| `turing`    | `scores`, `trend`, `dimensions`, `contact`, `trajectory`, `ab_tests`, `channel` |
| `tasks`     | `list`, `get`, `cancel` |
| `voice`     | `transcribe`, `session.start`, `session.stop`, `session.interrupt`, `audio.end`, `config`, `clone`, `tool_response` |
| `security`  | `cot.summary` |
| `mcp`       | `resources.list`, `prompts.list` |
| `sota`      | `metrics` (alias for `metrics.snapshot`) |
| Singletons  | `connect`, `health`, `capabilities` |

## Demo Gateway Rules

- `ui/src/demo-gateway.ts` is the mock client for offline UI development.
- When adding a new gateway method in C, **also** add the mock response in `demo-gateway.ts`.
- Mock response shapes must **exactly** mirror the real gateway's JSON structure.
- Every mock method needs realistic sample data — not empty arrays or minimal stubs.
- Demo mode must exercise all view states: populated data, empty collections, error paths.
