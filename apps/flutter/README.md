## Status: Deprecated

The Flutter app is a stub and is not being actively developed. Native iOS (SwiftUI)
and Android (Jetpack Compose) apps provide better platform-native UX and are the
recommended mobile surfaces.

Design tokens in `design_tokens.dart` are still auto-generated from the shared
token pipeline and can be used if the Flutter app is revived in the future.
