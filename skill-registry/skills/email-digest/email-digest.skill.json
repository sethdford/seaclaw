{
  "name": "email-digest",
  "version": "1.0.0",
  "description": "Summarize and prioritize unread emails into a daily digest",
  "author": "seaclaw",
  "tags": ["email", "productivity", "summary"],
  "parameters": {
    "max_emails": { "type": "integer", "default": 50 },
    "priority_labels": { "type": "array", "default": ["urgent", "important"] }
  }
}
