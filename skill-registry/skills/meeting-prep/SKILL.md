# Meeting Prep

Prepare briefs for upcoming meetings by pulling calendar events and related documents.

## Usage

Invoke with `seaclaw skills run meeting-prep` or configure as a cron job.

## Parameters

- `lookahead_hours` — How far ahead to scan for meetings (default: 24)
- `include_attachments` — Whether to include document attachments in briefs
